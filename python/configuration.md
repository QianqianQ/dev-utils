# Configuration

## linter
```bash
# Install python linter pylint flake8
# from vscode: /bin/python3 -m pip install -U flake8
python<version> -m pip install flake8
# add /home/user/.local/bin to $PATH to make whereis * works to find linters
# update vscode settings
```